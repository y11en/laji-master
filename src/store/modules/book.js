// 书籍模块
import service from '../../api'
import { Message } from 'element-ui'

const state = {
    
}

const mutations = {

}

const actions = {
    aaa(){
        console.log('ssssss')
    }
}

export default {
    namespaced: true,
    state,
    mutations,
    actions
}